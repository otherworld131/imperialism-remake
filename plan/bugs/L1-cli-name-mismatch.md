# L1. `build mill` / `build car` / `build unit militia` CLI name mismatch

**Severity:** Low / UX — FIXED

- [x] Added `build militia` alias for `build unit militia`
- [x] Added `build freight` alias for building freight cars
- [x] `build lumbermill`, `build lumber mill`, etc. already worked via existing parser
