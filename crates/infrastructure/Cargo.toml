[package]
name = "infrastructure"
version.workspace = true
edition.workspace = true

[dependencies]
application = { path = "../application" }
domain = { path = "../domain", default-features = false }
domain-snapshot = { path = "../domain-snapshot" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ron = "0.8"
rmp-serde = "1"
flate2 = "1"
zstd = "0.13"
